//ExStart
//ExSummary:本代码演示如何创建 Word 文档,并插入图像,并使用 Aspose.Words 进行浮动布局。
//ExStepSummary:0: 以下代码初始化了一个新的文档和文档构建器 。
//ExStepSummary:1: 以下代码在文档中插入图像。
//ExStepSummary:2: 以下代码将文档保存到指定位置 。
//ExStepImage:1:images/1.png
//ExStep:0-
// 创建新文档实例
Document doc = new Document();
DocumentBuilder builder = new DocumentBuilder(doc);

//ExStep:1-
// 从指定的路径插入图像
builder.InsertImage(ImagesDir + "Transparent background logo.png", 
    RelativeHorizontalPosition.Margin,
    100, 
    RelativeVerticalPosition.Margin, 
    100, 
    200, 
    100, 
    WrapType.Square); 

//ExStep:2-
// 将文档保存到指定文件
doc.Save("AddContentUsingDocumentBuilder.InsertFloatingImage.docx");
//ExEnd