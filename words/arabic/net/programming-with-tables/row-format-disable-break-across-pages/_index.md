---
"description": "تعرف على كيفية تعطيل فواصل الصفوف عبر الصفحات في مستندات Word باستخدام Aspose.Words لـ .NET للحفاظ على قابلية قراءة الجدول وتنسيقه."
"linktitle": "تعطيل تنسيق الصف عبر الصفحات"
"second_title": "واجهة برمجة تطبيقات معالجة المستندات Aspose.Words"
"title": "تعطيل تنسيق الصف عبر الصفحات"
"url": "/ar/net/programming-with-tables/row-format-disable-break-across-pages/"
"weight": 10
---

{{< blocks/products/pf/main-wrap-class >}}

{{< blocks/products/pf/main-container >}}

{{< blocks/products/pf/tutorial-page-section >}}

# تعطيل تنسيق الصف عبر الصفحات

## مقدمة

عند العمل مع الجداول في مستندات Word، قد ترغب في التأكد من عدم انفصال الصفوف بين الصفحات، وهو أمر ضروري للحفاظ على سهولة قراءة مستنداتك وتنسيقها. يوفر Aspose.Words لـ .NET طريقة سهلة لتعطيل انفصال الصفوف بين الصفحات.

في هذا البرنامج التعليمي، سنوضح لك عملية تعطيل فواصل الصفوف عبر الصفحات في مستند Word باستخدام Aspose.Words لـ .NET.

## المتطلبات الأساسية

قبل أن نبدأ، تأكد من أن لديك المتطلبات الأساسية التالية:
- تم تثبيت Aspose.Words لمكتبة .NET.
- مستند Word يحتوي على جدول يمتد على عدة صفحات.

## استيراد مساحات الأسماء

أولاً، قم باستيراد المساحات الأساسية اللازمة في مشروعك:

```csharp
using Aspose.Words;
using Aspose.Words.Tables;
```

## الخطوة 1: تحميل المستند

قم بتحميل المستند الذي يحتوي على الجدول الذي يمتد على عدة صفحات.

```csharp
// المسار إلى دليل المستندات الخاص بك 
string dataDir = "YOUR DOCUMENT DIRECTORY";

Document doc = new Document(dataDir + "Table spanning two pages.docx");
```

## الخطوة 2: الوصول إلى الجدول

الوصول إلى الجدول الأول في المستند. هذا يفترض أن الجدول الذي تريد تعديله هو الجدول الأول في المستند.

```csharp
Table table = (Table) doc.GetChild(NodeType.Table, 0, true);
```

## الخطوة 3: تعطيل تقسيم الصفحات لجميع الصفوف

قم بالمرور عبر كل صف في الجدول وتعيين `AllowBreakAcrossPages` الممتلكات إلى `false`. وهذا يضمن عدم انقطاع الصفوف عبر الصفحات.

```csharp
// تعطيل تقسيم الصفحات لجميع الصفوف في الجدول.
foreach (Row row in table.Rows)
    row.RowFormat.AllowBreakAcrossPages = false;
```

## الخطوة 4: حفظ المستند

احفظ المستند المعدل في الدليل المحدد.

```csharp
doc.Save(dataDir + "WorkingWithTables.RowFormatDisableBreakAcrossPages.docx");
```

## خاتمة

في هذا البرنامج التعليمي، شرحنا كيفية تعطيل فواصل الصفوف بين الصفحات في مستند Word باستخدام Aspose.Words لـ .NET. باتباع الخطوات الموضحة أعلاه، يمكنك ضمان بقاء صفوف الجدول سليمة وعدم انقسامها بين الصفحات، مما يحافظ على سهولة قراءة المستند وتنسيقه.

## الأسئلة الشائعة

### هل يمكنني تعطيل فواصل الصفوف عبر الصفحات لصف معين بدلاً من جميع الصفوف؟  
نعم، يمكنك تعطيل فواصل الصفوف لصفوف محددة عن طريق الوصول إلى الصف المطلوب وتعيينه `AllowBreakAcrossPages` الممتلكات إلى `false`.

### هل تعمل هذه الطريقة بالنسبة للجداول ذات الخلايا المدمجة؟  
نعم، هذه الطريقة تعمل مع الجداول ذات الخلايا المدمجة. الخاصية `AllowBreakAcrossPages` ينطبق على الصف بأكمله، بغض النظر عن دمج الخلايا.

### هل ستنجح هذه الطريقة إذا كان الجدول متداخلاً داخل جدول آخر؟  
نعم، يمكنك الوصول إلى الجداول المتداخلة وتعديلها بنفس الطريقة. تأكد من صحة مرجع الجدول المتداخل من خلال فهرسه أو خصائصه الأخرى.

### كيف يمكنني التحقق مما إذا كان الصف يسمح بالتقسيم عبر الصفحات؟  
يمكنك التحقق مما إذا كان الصف يسمح بالتقسيم عبر الصفحات عن طريق الوصول إلى `AllowBreakAcrossPages` ممتلكات `RowFormat` والتحقق من قيمتها.

### هل هناك طريقة لتطبيق هذا الإعداد على كافة الجداول في المستند؟  
نعم، يمكنك المرور عبر كافة الجداول في المستند وتطبيق هذا الإعداد على كل جدول.


{{< /blocks/products/pf/tutorial-page-section >}}


{{< /blocks/products/pf/main-container >}}

{{< /blocks/products/pf/main-wrap-class >}}


{{< blocks/products/products-backtop-button >}}