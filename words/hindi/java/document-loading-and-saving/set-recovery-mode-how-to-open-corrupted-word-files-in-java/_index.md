---
category: general
date: 2025-12-23
description: डैमेज़्ड वर्ड दस्तावेज़ों को पुनर्प्राप्त करने के लिए रिकवरी मोड सेट
  करें। जानें कि DOCX फ़ाइलें कैसे खोलें, रिकवरी मोड का उपयोग करें, और जावा में भ्रष्ट
  फ़ाइलों को कैसे संभालें।
draft: false
keywords:
- set recovery mode
- recover damaged word
- how to open docx
- open corrupted word file
- use recovery mode
language: hi
og_description: खराब Word दस्तावेज़ों को पुनर्प्राप्त करने के लिए रिकवरी मोड सेट करें।
  यह गाइड दिखाता है कि DOCX फ़ाइलें कैसे खोलें, रिकवरी मोड का उपयोग करें, और Java
  में भ्रष्ट फ़ाइलों को कैसे संभालें।
og_title: रिकवरी मोड सेट करें – जावा में भ्रष्ट वर्ड फ़ाइलें खोलें
tags:
- Java
- Aspose.Words
- Document Recovery
title: रिकवरी मोड सेट करें – जावा में करप्ट वर्ड फ़ाइलें कैसे खोलें
url: /hi/java/document-loading-and-saving/set-recovery-mode-how-to-open-corrupted-word-files-in-java/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# रिकवरी मोड सेट करें – जावा में करप्टेड वर्ड फ़ाइलें कैसे खोलें

क्या आपने कभी ऐसे वर्ड दस्तावेज़ पर **रिकवरी मोड सेट** करने की कोशिश की है जो खुल नहीं रहा? आप अकेले नहीं हैं। कई डेवलपर्स को तब समस्या आती है जब DOCX थोड़ा करप्ट हो जाता है और सामान्य `new Document("file.docx")` अपवाद फेंकता है। अच्छी खबर? Aspose.Words for Java आपको एक बिल्ट‑इन तरीका देता है **रिकवरी मोड उपयोग करने** का और वास्तव में **खराब वर्ड** फ़ाइलों को **रिकवर** करने का।

> **Pro tip:** यदि आप केवल छोटे‑छोटे गड़बड़ियों (जैसे गायब फुटर) से निपट रहे हैं, तो **Tolerant** रिकवरी मोड आमतौर पर पर्याप्त होता है। **Strict** को उन स्थितियों के लिए रखें जहाँ आपको प्रोसेसिंग से पहले दस्तावेज़ 100 % साफ़ चाहिए।

## आपको क्या चाहिए

- **Java 17** (या कोई भी हालिया JDK; API समान काम करता है)
- **Aspose.Words for Java** 23.9 (या नया) – वह लाइब्रेरी जो `LoadOptions` क्लास प्रदान करती है।
- परीक्षण के लिए एक **करप्टेड DOCX** फ़ाइल (आप वैध फ़ाइल को हेक्स एडिटर से ट्रंकेट करके बना सकते हैं)।
- आपका पसंदीदा IDE (IntelliJ, Eclipse, VS Code—जो भी आपको आरामदायक लगे)।

बस इतना ही। कोई अतिरिक्त Maven प्लगइन नहीं, कोई बाहरी यूटिलिटी नहीं। केवल कोर लाइब्रेरी और थोड़ा कोड।

![Illustration of setting recovery mode in Aspose.Words Java API](/images/set-recovery-mode-java.png){.align-center alt="रिकवरी मोड सेट करें"}

## चरण 1 – `LoadOptions` इंस्टेंस बनाएं

सबसे पहले आपको एक `LoadOptions` ऑब्जेक्ट इंस्टैंशिएट करना है। इसे एक टूलबॉक्स समझें जो Aspose.Words को बताता है **आने वाली फ़ाइल को कैसे ट्रीट करना है**।

```java
import com.aspose.words.LoadOptions;

// Step 1: Create LoadOptions with default settings
LoadOptions loadOptions = new LoadOptions();
```

इस चरण को क्यों छोड़ें? क्योंकि बिना `LoadOptions` के आप लाइब्रेरी को नहीं बता सकते कि आप **रिकवरी मोड उपयोग** करना चाहते हैं या नहीं। डिफ़ॉल्ट व्यवहार स्ट्रिक्ट है, जिसका मतलब है कि कोई भी करप्शन लोड को रोक देगा।

## चरण 2 – सही रिकवरी मोड चुनें

Aspose.Words दो enum वैल्यू प्रदान करता है:

| मोड | क्या करता है |
|------|--------------|
| `RecoveryMode.Tolerant` | जितना संभव हो उतना बचाने की कोशिश करता है। *recover damaged word* परिदृश्यों के लिए आदर्श जहाँ केवल कोई स्टाइल गायब है या रिलेशनशिप टूटी हुई है। |
| `RecoveryMode.Strict`   | किसी भी समस्या पर तुरंत फेल हो जाता है। तब उपयोग करें जब आपको दस्तावेज़ पूरी तरह साफ़ होने की गारंटी चाहिए प्रोसेसिंग से पहले। |

एक ही लाइन में मोड सेट करें:

```java
import com.aspose.words.RecoveryMode;

// Step 2: Tell the loader to be forgiving
loadOptions.setRecoveryMode(RecoveryMode.Tolerant); // or RecoveryMode.Strict
```

**क्यों महत्वपूर्ण है:** जब आप **रिकवरी मोड उपयोग** करते हैं, लाइब्रेरी आंतरिक रूप से टूटे हुए हिस्सों को पैच करती है, गायब XML नोड्स को पुनः बनाती है, और आपको एक उपयोगी `Document` ऑब्जेक्ट देती है। *strict* मोड में आपको `InvalidFormatException` मिलती है।

## चरण 3 – अपने विकल्पों के साथ दस्तावेज़ लोड करें

अब आप अंततः फ़ाइल को Aspose.Words को देते हैं, साथ में वहLoadOptions` पास करते हैं जिसे आपने अभी कॉन्फ़िगर किया है।

```java
import com.aspose.words.Document;

// Step 3: Load the (potentially corrupted) DOCX
String filePath = "C:/Documents/corrupted.docx";
Document doc = new Document(filePath, loadOptions);
```

यदि फ़ाइल केवल हल्की करप्टेड है, तो `doc` एक पूरी तरह कार्यशील `Document` ऑब्जेक्ट होगा। आप अब कर सकते हैं:

- पाठ पढ़ें (`doc.getText()`),
- किसी अन्य फ़ॉर्मेट में सेव करें (`doc.save("repaired.pdf")`),
- या `Document` API के माध्यम से रिकवर किए गए भागों की सूची भी देख सकते हैं।

### रिकवरी की पुष्टि

एक त्वरित sanity चेक आपको यह पुष्टि करने में मदद करता है कि रिकवरी वास्तव में सफल रही:

```java
if (doc.getSections().getCount() > 0) {
    System.out.println("Document loaded successfully – recovery mode worked!");
} else {
    System.out.println("No sections found – the file might be beyond repair.");
}
```

## चरण 4 – एज केस संभालें

### 4.1 जब Tolerant पर्याप्त नहीं है

कभी‑कभी फ़ाइल इतनी टूट जाती है कि **Tolerant** मोड भी उसे जोड़ नहीं पाता (जैसे, कोर XML गायब हो)। ऐसे दुर्लभ मामलों में आप कर सकते हैं:

1. **`RecoveryMode.Strict` के साथ दूसरी बार लोड करने का प्रयास** करें ताकि देखें कि एरर मैसेज अधिक विवरण देता है या नहीं।
2. **zip‑utility का उपयोग** करके मैन्युअली XML पार्ट्स निकालें और उन्हें ठीक करें।
3. **एक्सेप्शन लॉग** करें और उपयोगकर्ता को सूचित करें कि दस्तावेज़ रिकवरी योग्य नहीं है।

```java
try {
    loadOptions.setRecoveryMode(RecoveryMode.Tolerant);
    Document doc = new Document(filePath, loadOptions);
    // proceed with doc
} catch (Exception e) {
    System.err.println("Tolerant mode failed: " + e.getMessage());
    // optional: retry with Strict or alert the user
}
```

### 4.2 मेमोरी विचार

रिकवरी सक्षम करके बड़े DOCX फ़ाइलों को लोड करने से मेमोरी उपयोग अस्थायी रूप से दोगुना हो सकता है क्योंकि Aspose.Words मूल और रिकवर किए गए दोनों स्ट्रक्चर मेमोरी में रखता है। यदि आप बड़े बैच प्रोसेस कर रहे हैं:

- **एक ही `LoadOptions` इंस्टेंस को पुन: उपयोग** करें, हर बार नया बनाने के बजाय।
- **`Document` को डिस्पोज़** करें (`doc.close()`) जैसे ही काम समाप्त हो।
- **पर्याप्त हीप वाले JVM पर चलाएँ** (`-Xmx2g` या बड़े मल्टी‑गिगाबाइट फ़ाइलों के लिए अधिक)।

### 4.3 रिकवर की गई फ़ाइल को सेव करना

सफल लोड के बाद, आप **साफ़ संस्करण को सेव** करना चाह सकते हैं ताकि भविष्य में फिर से रिकवरी चलाने की ज़रूरत न पड़े।

```java
String repairedPath = "C:/Documents/repaired.docx";
doc.save(repairedPath);
System.out.println("Repaired file saved to: " + repairedPath);
```

अब अगली बार जब आप `repaired.docx` खोलेंगे, तो आप **रिकवरी मोड उपयोग** चरण को पूरी तरह छोड़ सकते हैं।

## अक्सर पूछे जाने वाले प्रश्न

**प्रश्न:** क्या यह पुराने `.doc` फ़ाइलों के लिए भी काम करता है?  
**उत्तर:** हाँ। वही `LoadOptions` तरीका `.doc` और `.rtf` पर भी लागू होता है। केवल फ़ाइल एक्सटेंशन बदलें।

**प्रश्न:** क्या मैं `setRecoveryMode` को अन्य लोडिंग विकल्पों (जैसे पासवर्ड) के साथ संयोजित कर सकता हूँ?  
**उत्तर:** बिल्कुल। `LoadOptions` में `setPassword` और `setLoadFormat` जैसी प्रॉपर्टीज़ हैं। `setRecoveryMode` को कॉल करने से पहले इन्हें सेट करें।

**प्रश्न:** क्या कोई प्रदर्शन लागत है?  
**उत्तर:** थोड़ा—रिकवरी पार्सिंग ओवरहेड जोड़ती है। बेंचमार्क में, 5 MB करप्टेड फ़ाइल **Tolerant** मोड में लगभग 30 % धीमी लोड होती है तुलना में स्ट्रिक्ट लोडिंग के साथ साफ़ फ़ाइल की। अधिकांश बैच जॉब्स के लिए अभी भी स्वीकार्य है।

## पूर्ण कार्यशील उदाहरण

नीचे एक पूरा, रन‑टू‑रन Java क्लास है जो दर्शाता है **docx कैसे खोलें**, **रिकवरी मोड उपयोग करें**, और **रिकवर की गई कॉपी सेव करें**।

```java
package com.example.recovery;

import com.aspose.words.*;

public class RecoverCorruptedDocx {
    public static void main(String[] args) {
        // Path to the possibly corrupted DOCX
        String inputPath = "C:/Documents/corrupted.docx";
        // Where the repaired file will be saved
        String outputPath = "C:/Documents/repaired.docx";

        // 1️⃣ Create LoadOptions
        LoadOptions loadOptions = new LoadOptions();

        // 2️⃣ Choose recovery mode – Tolerant is usually enough
        loadOptions.setRecoveryMode(RecoveryMode.Tolerant);
        // If you need strict validation, switch to RecoveryMode.Strict

        try {
            // 3️⃣ Load the document with the configured options
            Document doc = new Document(inputPath, loadOptions);

            // Quick sanity check
            if (doc.getSections().getCount() > 0) {
                System.out.println("✅ Document loaded – recovery succeeded.");
            } else {
                System.out.println("⚠️ No sections found – the file may be beyond repair.");
            }

            // 4️⃣ (Optional) Save a clean copy for future use
            doc.save(outputPath);
            System.out.println("💾 Repaired file saved to: " + outputPath);
        } catch (Exception e) {
            // Handle cases where even tolerant mode fails
            System.err.println("❌ Failed to load document: " + e.getMessage());
            // You could retry with Strict or log for further analysis
        }
    }
}
```

इस क्लास को Aspose.Words for Java JAR को प्रोजेक्ट के क्लासपाथ में जोड़ने के बाद चलाएँ। यदि इनपुट फ़ाइल केवल थोड़ा क्षतिग्रस्त है, तो आपको **✅** संदेश और डिस्क पर एक नई `repaired.docx` दिखाई देगी।

## निष्कर्ष

हमने वह सब कवर किया जो आपको **रिकवरी मोड सेट** करने और जावा में सफलतापूर्वक **करप्टेड वर्ड** फ़ाइलें खोलने के लिए चाहिए। `LoadOptions` ऑब्जेक्ट बनाकर, उपयुक्त `RecoveryMode` चुनकर, और कभी‑कभी आने वाले एज केस को संभालकर आप “फ़ाइल नहीं खुल रही” की निराशा को एक सुगम रिकवरी वर्कफ़्लो में बदल सकते हैं।

याद रखें:

- **Tolerant** अधिकांश *recover damaged word* परिदृश्यों के लिए आपका गो‑टू है।  
- **Strict** आपको हार्ड फेल देता है जब आपको पूर्ण निश्चितता चाहिए।  
- हमेशा लोड किए गए दस्तावेज़ की पुष्टि करें और संभव हो तो भविष्य के रन के लिए एक साफ़ कॉपी सेव करें।

अब आप आत्मविश्वास से जवाब दे सकते हैं “**docx कैसे खोलें** जो लोड नहीं हो रहा?” एक ठोस कोड स्निपेट और स्पष्ट व्याख्या के साथ। कोडिंग का आनंद लें, और आपके दस्तावेज़ स्वस्थ रहें!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}